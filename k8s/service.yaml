apiVersion: v1
kind: Service
metadata:
  name: privacy-devsecops-demo
spec:
  selector:
    app: privacy-devsecops-demo
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
